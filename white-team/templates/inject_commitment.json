{
  "_README": [
    "PUBLIC commitment object. aegis.inject-commitment.v2.",
    "Schema + 12 verification steps: ../config/inject_commitment.schema.json",
    "SECRET opening material (salt + package): ../config/inject_opening.schema.json",
    "Spec: ../../00-shared/18_exercise_assurance.md",
    "",
    "!! v1 IS DEPRECATED -- its public nonce provided NO hiding, and was not even in the",
    "   digest preimage. Do not use it.",
    "",
    "COMMIT:  publish THIS object. Withhold the package AND the opening_salt.",
    "REVEAL:  publish the package and opening_salt; White recomputes and compares.",
    "",
    "c = SHA-384( u64be(len(domain))  || 'aegis.inject-commitment.v2' ||",
    "             u64be(len(package)) || JCS(canonical_package)       ||",
    "             u64be(len(salt))    || opening_salt )",
    "",
    "Length framing is 8-byte big-endian. Raw concatenation is NOT acceptable -- it admits",
    "boundary-shifting between adjacent fields.",
    "Remove _README before signing if your canonicalizer rejects unknown fields."
  ],

  "type": "aegis.inject-commitment.v2",
  "commitment_id": "IC-EX-YYYY-NNN-001",
  "exercise_id": "EX-YYYY-NNN",
  "commitment_algorithm": "SHA-384",
  "commitment_digest": "<96 lowercase hex chars over the LENGTH-FRAMED preimage>",
  "canonicalization": "JCS",
  "package_schema": "aegis.inject-package.v1",
  "package_version": 1,
  "created_at": "YYYY-MM-DDTHH:MM:SSZ",
  "valid_from": "YYYY-MM-DDTHH:MM:SSZ",
  "reveal_deadline": "YYYY-MM-DDTHH:MM:SSZ",
  "exercise_assurance_key_id": "<key-id>",
  "previous_commitment": null,
  "signature": "<EA signature over JCS of all public fields except 'signature'>",

  "_CHECKLIST_AT_COMMITMENT": [
    "[ ] opening_salt generated from a CSPRNG, 32 or 48 bytes",
    "[ ] Salt checked against the SALT REGISTRY -- never reused (needed for verify step 11)",
    "[ ] Opening material stored under EA control ONLY, and BACKED UP",
    "    (losing the salt = permanently unopenable = looks identical to refusing to reveal)",
    "[ ] Package canonicalized with JCS before hashing",
    "[ ] Preimage LENGTH-FRAMED, not concatenated",
    "[ ] Digest recomputed independently by a second method",
    "[ ] reveal_deadline set -- an unrevealed package past deadline is VOID, not pending",
    "[ ] Published to White BEFORE exercise start (this is verify step 4, and it is also",
    "    your independent temporal anchor if the signing key is ever disputed)",
    "[ ] previous_commitment + supersession_reason set if superseding"
  ],

  "_CHECKLIST_AT_REVEAL": [
    "[ ] 1.  Public commitment schema valid",
    "[ ] 2.  EA signature verifies over all public fields except 'signature'",
    "[ ] 3.  type, exercise_id, key ID, algorithm, validity window confirmed",
    "[ ] 4.  Commitment published BEFORE exercise execution",
    "[ ] 5.  Opening uses the expected package schema",
    "[ ] 6.  Revealed package canonicalized with the declared algorithm",
    "[ ] 7.  Length-framed preimage reconstructed",
    "[ ] 8.  Digest recomputed and CONSTANT-TIME compared",
    "[ ] 9.  Inject identifiers, count, ordering rules, timestamps validated",
    "[ ] 10. Supersession chain validated",
    "[ ] 11. opening_salt absent from every other commitment",
    "[ ] 12. Commitment + opening + package + verification result preserved as evidence",
    "ANY failure -> inject package invalid -> report under EA-6",
    "",
    "KEY STATUS: evaluate as at created_at. Routine expiration/rotation does NOT invalidate.",
    "Known or possible compromise BEFORE created_at -> SIGNATURE_VALID_BUT_ASSURANCE_DISPUTED",
    "-> manual assurance review. See ../../00-shared/19_aegis_trust_model.md sec 20.5a"
  ]
}
